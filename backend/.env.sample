HF_API_KEY="YOUR_HUGGINGFACE_API_KEY"

# ── Image Captioning (HuggingFace BLIP) ──────────────────────
HF_CAPTION_MODEL="Salesforce/blip-image-captioning-large"
HF_TIMEOUT=90

# ── OCR (EasyOCR — runs locally, no network needed) ──────────
# Language code for OCR. "eng" = English.
OCR_LANGUAGE=eng